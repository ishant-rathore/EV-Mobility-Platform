# Mock and Demo Data

Every file in this directory is deterministic hackathon fallback data, not a live network feed. Records include `source: "demo"` and `isSimulated: true`. Runtime adapters currently keep their tested TypeScript fixtures; a future loader may make these JSON files canonical without changing API responses.
