# Backend

<<<<<<< HEAD
EV Mobility Platform monorepo domain.
=======
`api` is the modular-monolith application: routes validate HTTP input, controllers/routes delegate, services orchestrate use cases, and repositories/providers own persistence or external integration. Pure calculations are imported from `intelligence` packages.

The existing module names such as `ev`, `journey`, `routing`, `booking`, and `reliability` are intentionally retained to preserve imports and public API behavior.
>>>>>>> junior/main
