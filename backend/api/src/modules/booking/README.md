# Booking

Module 9 demo reservation lifecycle, validation, and conflict prevention live here. The current repository is intentionally in memory and every response is labelled `DEMO`; replace it with a Prisma adapter only after the schema can persist recommendation and charger references safely.
