# GitHub Workflows

GitHub only executes workflows from the repository-root `.github/workflows` directory, which remains the canonical location. Architecture-specific workflow splits can be added there when they provide independent checks; configs are not duplicated here.
