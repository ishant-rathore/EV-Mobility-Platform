# Contributing

Keep changes scoped to one domain module, preserve dependency direction, validate external input, and add tests for behavior changes.

Before opening a pull request, run:

```bash
npm run typecheck
npm test
npm run build
```
