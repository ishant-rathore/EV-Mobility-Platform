# Workers

Background work remains in-process until a workload justifies extraction. Planned boundaries are station synchronization, analytics rollups, notifications, and telemetry processing; no queue or Redis dependency is introduced.
