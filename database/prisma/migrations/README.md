# Prisma Migrations

No schema migration was created for repository restructuring because database semantics were intentionally unchanged. Generate migrations only for reviewed schema changes with an explicitly selected development database.
