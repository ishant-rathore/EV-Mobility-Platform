# WebSocket Compatibility Boundary

The working Socket.IO implementation remains in `src/realtime` to preserve imports. New client-event documentation should reference that implementation until a behavior-driven rename is justified.
