# Middleware

Cross-cutting HTTP concerns belong here. Error translation was extracted without changing response shapes. Authentication, roles, validation, rate limiting, request IDs, and audit logging should be added only with tested runtime behavior rather than empty files.
