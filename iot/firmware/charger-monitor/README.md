# ESP8266 charger prototype

Copy `include/config.example.h` to `include/config.h`, provide local Wi-Fi/MQTT settings, and never connect hobby GPIO or breadboards directly to energized EV charging conductors.
