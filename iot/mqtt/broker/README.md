# Broker

Local Eclipse Mosquitto configuration is maintained in `infrastructure/mqtt`. Production authentication and authorization are deployment responsibilities; anonymous access is for isolated local development only.
