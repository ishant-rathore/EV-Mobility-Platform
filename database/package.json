{
  "name": "@ev-mobility/database",
  "version": "0.1.0",
  "private": true,
  "description": "Prisma schema, migrations, seeds, and database operations for EV Mobility Platform",
  "scripts": {
    "dev": "echo dev",
    "build": "echo build",
    "test": "echo test",
    "lint": "echo lint",
    "typecheck": "echo typecheck",
    "verify": "tsx scripts/verify.ts"
  },
  "dependencies": {
    "@prisma/client": "6.12.0"
  },
  "devDependencies": {
    "tsx": "^4.20.5"
  }
}
