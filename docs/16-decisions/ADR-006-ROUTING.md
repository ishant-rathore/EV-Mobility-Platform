# ADR-006: Routing Providers and Fallback

**Status:** Accepted

Use provider interfaces with OpenStreetMap/OSRM-compatible routing and deterministic demo fallback. Preserve the working `routing` backend module name. Route diversification is an advisory simulation and does not control public-road traffic.
