# Frontend Features

Feature folders are added only when a workflow owns state, hooks, schemas, and components. Existing implementation remains organized by pages/components until a real feature extraction is justified.
