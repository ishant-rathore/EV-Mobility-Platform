# API Contracts Package Boundary

HTTP response contracts currently remain close to their backend modules, with genuinely shared shapes in `@ev-mobility/shared-types`. Add endpoint-specific contracts here as frontend/backend duplication appears.
