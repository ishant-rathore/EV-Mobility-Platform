# Energy Engine

Pure, deterministic battery and route-energy calculations extracted from the API. It has no HTTP, database, or provider dependencies. Results are estimates and must be labelled accordingly in user-facing experiences.
