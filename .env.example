NODE_ENV=development
PORT=3000
WEB_ORIGIN=http://localhost:5173
DATABASE_URL=postgresql://volttwin:volttwin@localhost:5432/volttwin
MQTT_URL=mqtt://localhost:1883
ENABLE_MQTT=true
VITE_API_URL=http://localhost:3000/api/v1
VITE_WS_URL=http://localhost:3000

# When unset/false, EV vehicles are served from the in-memory demo repository
# instead of PostgreSQL (see Demo Rules — the driver flow must survive a dead DB).
EV_USE_DATABASE=false
